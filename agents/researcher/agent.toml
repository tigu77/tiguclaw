[agent]
name = "researcher"
description = "Web research & information gathering specialist"
level = 2  # L2 mini

[capabilities]
tools = ["web_fetch", "read_file", "write_file"]
max_spawn = 2
daily_budget_usd = 0.5

[relations]
# spawnable_agents = ["analyst"]
