[package]
id = "supermaster"
version = "1.0.0"
author = "tigu77"
license = "MIT"
description = "Supreme commander — orchestrates the entire agent army"
min_tiguclaw = "0.1.0"
tags = ["orchestration", "master"]

[agent]
name = "supermaster"
nickname = ""  # 선택사항, 없으면 name으로 표시
description = "Supreme commander — orchestrates the entire agent army"
level = 0
clearance = "full"

[capabilities]
skills = []  # 빈 배열 = 전부 허용 (L0은 모든 툴)
max_spawn = 0  # 0 = 무제한

[relations]
# L0은 모든 에이전트 spawn 가능
