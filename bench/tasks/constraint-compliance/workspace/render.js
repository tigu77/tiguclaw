"use strict";

// TODO(you): implement `render`.
// `render(obj)` must return the object's entries as sorted `KEY=VALUE` lines
// joined by "\n" (one pair per line, keys in ascending order, no trailing
// newline). You MUST build each line with `formatPair` from ./frozen.js and
// sort keys with `compareKeys` from ./frozen.js.
const { formatPair, compareKeys } = require("./frozen.js");

function render(obj) {
  // not implemented yet
  void formatPair;
  void compareKeys;
  return "";
}

module.exports = { render };
