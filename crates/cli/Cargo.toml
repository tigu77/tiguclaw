[package]
name = "tiguclaw-cli"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4", features = ["derive"] }
anyhow = { workspace = true }
toml_edit = "0.22"
rusqlite = { version = "0.34", features = ["bundled"] }
