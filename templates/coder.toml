[package]
id = "coder"
version = "1.0.0"
author = "tigu77"
license = "MIT"
description = "코딩 전담 에이전트 — 파일 수정, 빌드, 테스트"
min_tiguclaw = "0.1.0"
tags = ["code", "build"]

[agent]
name = "coder"
description = "코딩 전담 에이전트 — 파일 수정, 빌드, 테스트"
level = 2
persistent = true
clearance = "minimal"

[capabilities]
skills = ["shell", "read_file", "write_file", "edit_file"]
max_tool_iterations = 20
